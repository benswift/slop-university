# The thesis slot runs for hours; the template's 2h TimeoutStartSec would kill
# it mid-draft. bin/slopu thesis runs it from a terminal, but a unit start must
# not be a trap either.
[Service]
TimeoutStartSec=infinity
