---
description: Generate a satirical ANU-style document from a named preset (e.g. strategy, impact-report) and a steering prompt
---

Read `skills/from-preset/SKILL.md` and execute its workflow end-to-end. Argument: $ARGUMENTS (first whitespace-separated word is the preset name; the rest is the steering prompt).
